FUNCTIONAL: 
-Make the scraper modular by dividing it into functions, which makes testing and understanding the code much easier, 
specially for extracting information from a product (make it supermarket agnostic)
-Use run_scraper_pipeline to make the scraping
-If something in the scraper fails, retry that component 3 times if it's something normal and 5 if there's more variability (LLMs),
each function should have its max retries 
-Remove grasas_mono y grasas_poli from the schema, prompt and ground truth dataset as it's only presents in oil (not worth it calculating)
-The eval can't depend on a test, I should create a utils function or something that handles getting information from a single product
-Add a verbose tag to show the predicted and extracted information if not hide it
-Test only a couple of things (allergens, nutritional information) not all the features of the test through the terminal 
-Add reflexion to the allergens as it's something critical more valuable than the relative price (which might be biased) or the claims (extra information)



ARCHITECTURE: 
-Use the async engine from PostgreSQL -> NeonDB
-Use LangChain instead of manually handling every connection 
-Use LLamaIndex to handle the vector database connection and retrieving 
-Migrate the vector database to qdrant
-Create a syncronous version of the scraper and implement multithreading (for the LLMs it doesn't make sense as it is I/O bounded)